query ($user: String!, $count: Int = 100, $cursor: String) {
  user(login: $user) {
    repositories(first: $count, after: $cursor) {
      nodes {
        name
      }
      pageInfo {
        hasNextPage
        endCursor
      }
    }
  }
}
